/**
 * Точка входа для API-клиента.
 *
 * Модули разбиты по доменам (см. src/api/):
 *  - shared   — ядро запросов, сессия, общие типы;
 *  - field    — мобильное приложение оценивающего;
 *  - admin    — панель администратора;
 *  - registry — реестр/Квыр.
 *
 * Этот файл сохранён как re-export для обратной совместимости
 * со старыми импортами `from "./api"`.
 */
export * from "./api/index";

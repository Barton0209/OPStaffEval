ОТЧЁТ ПО АУИТУ System_Ocenok / KingiseppApp
Фиксация состояния (точка отката)
Git: ветка main (origin/main), HEAD f5a33873.сть незакоммиченные изменения (~40 файлов: backend/app/, frontend/src/, alembic/ и новые API-модули — не в git). Перед правками рекомендую коммит или stash.
Тесты: pytest backend/tests — 42 passed (baseline зелёный).
AGENTS.md: в проекте отсутствует (проверено glob'ом) — использовал README.md как контекст.
БД: KingiseppApp/dataingisepp.db, alembic head f1a2b3c4d5e6 — схема применена полностью.
ЭТАП 1. Карта проекта
Бэд (backend/app/, ~5.3k строк): main.py (lifespan, CORS, security headers, SPA-fallback), security.py (JWT/bcrypt), deps.py (RBAC), config.py API-роутеры: field.py(972),admin.py(1292),registry.py(814),groups.py(549),summary.py, exports.py, admin_imports.py, auth_control.py, economist.py, notifications.py. Сервисы: imports.py (1552), evaluations.py, tariff.py, events.py. Тесты: 10 файлов. Alembic: 4 миграции.

Фронтенд (frontend/src/): main (роутинг по ролям), LoginScreen.tsx, AdminApp.tsx + admin/*Panel.tsx, FieldApp.tsx, RegistryApp.tsx, ManagementApp.tsx, EconomistApp.tsx, api/ (/field/admin/registry/economist/notifications), offline.ts (Dexie), public/sw.js.

Авторизация: POST /api/field/auth/login (field.py:112), каскад «Контоль» — auth_control.py, смена пароля — field.py:150, auth_control.py:241.
Импорт: POST /api/admin/import/* (admin.py:173-306, admin_imports.py:60 логика — services/imports.py.
Отчёты: exports.py (Excel/PDF), registry.py (export.xlsx, pdf), summary.py.
ЭТАП 2-4. Найден проблемы
КРИТИЧЕСКИЕ
К1. Захват любой учётной записи через setup-password — auth_control.py:241-276 POST /api/auth/control/setup не требует аутентификации и принимает произвольный user_id. Проверки, что пароль ещё не установлен или must_change_password=True, нет — любой в LAN перезаписать пароль любого пользователя (включая ADMIN-OP) и сразу получить JWT (строка 265). Rate-limit 3/min не мешает одному запросу. Исправ: отклонять запрос, если user.password_hash задан и must_change_password=False; для первого входа требовать знание tab_no + одноразовый код от администратора.

 = db.get(User, user_id)
if not user or user.status != UserStatus.active:
    raise HTTPException(404, "Пользователь не найден")
if user.password_hash and not user.must_change_password:
    raise HTTPException(403,Пароль уже установлен. Сброс — только через Администрацию ОП")
Тесты: setup-password для пользователя с паролём → 403; для must_change_password=True → 200; инкрем token_version.

К2. Весь роутер /api/groups без авторизации — groups.py:49,64,106,140,224,303,330,378,429,467 CRUD групп доступов,вязка пользователей к группам, удаление — всё без Depends. Любой в сети может перестроить структуру Territory→Role→Group и читать фотоотрудников (персданные, 152-ФЗ). Path traversal в фото частично закрыт (496, 522), но list_photos(folder=...) (468-475) проверки .. не имеет. *Исправление:* require_roles(admin, admin_op, cok_okit)на все изменения; чтение — минимумget_current_user; публичными оставить только то, что нуж экрану входа (он уже дублируется в auth_control). Добавить проверку ..вlist_photos`. Тесты: все CRUD-эндпоинты без ток → 401; с ролью master → 403.

К3. Реальные пароли в репозитории — backend/scripts/smoke_full.py:35,46 Захардкожены ADMIN-OP / AdminOP2026 ВМ-0140784 / K0140784. Репозиторий запушен на GitHub (origin/main). Исправление: читать из env (SMOKE_ADMIN_PASSWORD), сменить обаоля в проде, проверить git-историю. Тесты: grep-чек в CI на паттерны паролей.

ВЫСОКИЕ
В1. Дублирующийут импорта — активна версия БЕЗ валидации файла — admin_imports.py:60 vs admin.py:195 Оба регистрируют POST /api/admin/import/upload. Вmain.py:182-185admin_imports включён **раньше** и перекрывает admin.py. Версия admin.py:215-234 проверяет magic bytesPK\x03\x04и лимит_upload_mb; активная версия admin_imports.py:95-96 — **нет**: принимает любой файл любого размера, записывает на диск до проверки block`, при неизвестном блоке файлстаётся на диске (119). Исправление: оставить один endpoint (с валидацией сигнатуры, размера, расширения), второй удалить; запись на д — только после проверки block/slot.

В2. Публичное перечисление пользователей — auth_control.py:121-152 GET /api/auth/control/users без авторизации отдаёт id,_no, ФИО, has_password всех активных пользователей — база для подбора и для атаки К1. Исправление: не отдавать has_password и внутренний ``; рассмотреть капчу/серверный секрет для каскада.

В3. Пароль bootstrap-админа теряется — main.py:31-77 Лог пиш «Пароль выдан отдельно», но temp_pwd никуда не выводится (в отличие от init_db.py:69, который печатает в консоль). README:27 утвержет «логируются в logs/app.log» — не соответствует коду. При установке через start.bat учётка ADMIN-OP создаётся с неизвестным паролем. Исправление: выить однократно в консоль/отдельный файл с правами 600, либо требовать ADMIN_INITIAL_PASSWORD из env. Заодно привести README в соответствие.

В. Дубли в архиве уволенных при повторном импорте — imports.py:982-1009 При наличии даты увольнения создаётся FiredEmployee безерки на существующую запись → каждый повторный импорт базы 1С плодит дубликаты. Потери данных нет (сотрудник остаётся в employees соstate="Уволен", оценки/назначения сохраняются — это плюс), но: уволенный продолжает числиться в назначениях и может оцениваться. *Испление:* upsert в fired_employees по (org, tab_no); при архивации снимать evaluate` с активных назначений. Тесты: повторный импорт одного файла → 1 запись fired_employees; назначения уволенного evaluate=False.

В5. Обход rate-limit через X-Forwarded-For — rate_limit.py:7-9 Ключ лимита берётся из XFF без провки, что запрос пришёл от доверенного прокси. При прямом доступе к uvicorn (порт 8000) заголовок подменяется → лимиты login (5/min) обходят. Исправление: доверять XFF только за прокси (проверка request.client.host в списке trusted proxies) или ключ по реальному client.host.

В6. Эскалацияолей через patch_user — admin.py:917-923, schemas.py:255 admin_op не может назначить только admin — но может назначить cok_okit (полный доступ к экспортам всем площадкам) и management_op. Плюс status принимается произвольной строкой — можно записать мусор, сломав вход пользователя. Исправление: whitelist ролей для admin_op (как в create_user:984-992); status — Enum UserStatus.

СРЕНИЕ
С1. Несогласованность хранилища токена на фронтенде — api/shared.ts:7 (sessionStorage) против ManagementApp.tsx:29,38,107,,128, admin/SettingsPanel.tsx:419,739,757, main.tsx:89 (localStorage). Эти экраны читают localStorage("kingisepp_token") → всегда null → null → 401. Функционально сломаны: приложение Руководства и блок архива уволенных в Настройках. Исправление: все обращения — через getToken()/`request из shared.ts.

С2. Временные Excel-файлы не удаляются — exports.py:107-116, admin_imports.py:300-308 tempfile.mkstemp + FileResponse без удал → накопление в temp. Исправление: BackgroundTask на удаление или StreamingResponse из BytesIO.

С3. Мёртвый код и захардкоженный период — imports.py:-183 _ensure_period_for_org: строки 168-183 недостижимы (после return); код периода "2026-H2" захардкожен — в 2027 создастся пери с прошлогодним кодом.

С4. Нет обработки 401 на фронтенде — shared.ts:87-102: ошибка просто пробрасывается; при инвалида токена (token_version) пользователь видит текст ошибки вместо разлогина.

С5. Персданные в IndexedDB без шифрования — offline.ts: outbox с оценками срудников хранится открыто на устройстве. Для Android — приемлемо при ОС-шифровании, но стоит зафиксировать как осознанный риск (152-З).

НИЗКИЕ
groups.py:23,35 — двойное создание APIRouter (косметика); exports.py:322-323 — импорты в конце фай.
imports.py:1399 — запутанное условие if not row or not row[c_tab] if c_tab else False (работает, но хрупко).
scheduler.py:61-69 —ение DomainEvent старше 90 дней: аудит-лог действий с персданными исчезает — согласовать с требованиями хранения.
cookie_secure=False по умолчан — нормально для LAN/HTTP, при переходе на HTTPS (Caddyfile :8443) включить.
ЭТАП 4. Согласованность данных
Модели ↔ миграции реальная БД: полное соответствие. Все 24 таблицы на месте, alembic_version = f1a2b3c4d5e6 (head). Колонки employees (cithip, category, probation_end_date, rate_last_raised) и users (must_change_password, token_version) покрыты миграциями + страховочными ALTER в main.py:80-105/init_db.py:-42 (избыточно, но идемпотентно). data/kingisepp_backup.db — старый снапшот (12 таблиц) — не путать с продом. Целостность: FK без on_delete cascade; физических удалений сотрудников в коде нет (soft-delete через state) — осиротевших записей текущая логика не создаёт- Excel ↔ БД: ⚠️ не хватает данных. Файлы Files/Ежедневная_выгрузка.xlsx и Files/Сводная_тарифная_сеткаlsx — стабы по 1-2 строки: «Ежедневная» имеет заголовки в строке 1 (Табельный/ФИО/Должность/УчастокПрораб — формат import_daily_assignees), а import_daily_v2 ожидает заголовки на строке 5 и другой набор колонок (C=Площадка, D=абельный…). Лист тарифной сетки называется Sheet, а не «Все данные» (код делает fallback на первый лист — сработает). Нужны реальные продфайлы и ваше описание форматов для верификации маппинга — сейчас подтвердить корректность по образцам нельзя.
ЭТАП 5.лан исправлений (на утверждение)
#	Пункт	Трудоёмкость
1	К1: защита setup-password (проверка has_password/must_change) + тесты	1-2 ч
2	К2: авторизация на /api/groups + тесты 401/403	2-3 ч
3	К3: убра пароли из smoke_full.py, сменить скомпрометированные пароли	1 ч + регламент
4	В1: слияние дублирующихся /import/upload в один салидацией	2-3 ч
5	В2: урезать ответ /auth/control/users	0.5 ч
6	В3: выдача bootstrap-пароля (env/соль) + правка README	1 ч
7	В4: dedup fired_employees + снятие evaluate с уволенных + тесты	2-3 ч
8	В5: trusted-proxy rate_limit	0.5-1 ч
9	В6: whitelist ролей в patch_user + Enum для status	1 ч
10	С1: миграция фронтенда на getToken() из shared.ts	1-2 ч
11	С2: удаление temp-файлов после отдачи	0.5 ч
12	С3, С4, низкие: чистка мёртвого кода, обработка 401	1-2 ч
Новые тесты: test_auth_control.py (К1, В2 — setup-password отказ, users без has_password), расшить test_security.py (К2 — 401/403 на groups), test_daily_import.py (В4 — идемпотентность увольнений), test_admin_imports.py (В1 — отказ не-Excel сигнатуру и >10МБ через активный роут).

Чего не хватило для полноты: реальные прод-файлы Excel (есть только стабы), доступ к . (не читал намеренно — есть SECRET_KEY-валидация в config.py:39-44), ваше описание форматов листов.

Код исправлений не пишу до вашеговерждения. Скажите, какие пункты включаем в первую итерацию (рекомендую К1-К3 + В1 как минимум).
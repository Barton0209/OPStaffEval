ПОШАГОВЫЙ ПЛАН РЕАЛИЗАЦИИ (Опирайся на код выше)
ШАГ 1: Роль "Экономист" и История ЧТС
Добавить роль economist в enum UserRole (в models.py). Обновить проверки в deps.py, если нужно.
Создать модель RateHistory (id, employee_id (FK), old_rate, new_rate, changed_at (Date), changed_by (User ID)).
Добавить в Employee поля: category (String, разряд) и probation_end_date (Date, дата конца испытательного срока). Создать Alembic миграцию.
ШАГ 2: Вкладка Экономиста (Ежемесячное обновление ЧТС)
Создать отдельный роутер api/economist.py (доступ только роли economist).
Эндпоинт POST /api/economist/update-rate: принимает табельный номер, дату изменения, новую ЧТС. Ищет сотрудника, обновляет ЧТС (hourly_rate), проставляет rate_last_raised, пишет запись в RateHistory.
UI: Создать вкладку EconomistPanel.tsx с таблицей для ввода (Табельный -> ФИО автоподстановка -> Дата -> Новая ЧТС).
Уведомления: Выводить бейдж для экономиста, если появились новые сотрудники, у которых ЧТС = 0 или не заполнена (hourly_rate is None).
ШАГ 3: Расширение Реестра (на основе registry.py)
В выдаче реестра (_collect) добавить поля: tariff_min, tariff_max (вилка согласно должности и гражданству из TariffGrid). Они уже частично есть, убедиться, что выводятся.
Добавить индикатор испытательного срока (сравнить текущую дату с probation_end_date).
Объединить логику колонок "Замещение" и "Тот, у кого работает" в одно поле в UI.
Права site_chief (КРИТИЧНО): Модифицировать ответ API. В реестре (_collect) и анкете (questionnaire) для роли site_chief СТРОГО ЗАПРЕТИТЬ отображение оценок Мастера/Прораба (поле primary_ev, secondary_ev, p_avg, s_avg). Начальник участка должен видеть только свою оценку и итоговую (final_score).
Убрать лишние данные из общих ячеек реестра (оставить только для админов).
ШАГ 4: Система уведомлений (In-App)
API: Создать GET /api/notifications, который возвращает счетчик непросмотренных/неоцененных анкет для текущего пользователя (мастера/начальника). Запрос должен искать Assignment, где пользователь является primary_user_id или secondary_user_id, а Evaluation в статусе draft или отсутствует.
UI: Вывести колокольчик/бейдж с количеством задач в шапке приложения (AdminApp.tsx и FieldApp.tsx).
ШАГ 5: Упрощение импорта для Админов (Ежедневная выгрузка)
Создать POST /api/admin/import/daily-assignees для загрузки одного Excel без сложных настроек (Табельный, ФИО, Должность, Участок, Прораб).
БД сама ищет сотрудника по табельному, обновляет его ФИО/должность и назначает к нужному прорабу (Assignment) согласно файлу.
ШАГ 6: Печать анкеты
Добавить кнопку "Печать / Экспорт PDF" в полностью оцененной анкете (использовать существующую логику export_questionnaires_pdf, но для одной карточки).
ТРЕБОВАНИЯ К КОДУ
Строго соблюдать RBAC. Использовать batch-загрузки (избегать N+1 в реестре, как уже сделано через load_grid_index).
Начни с Шага 1: прочитай models.py, добавь новые поля и таблицу, сгенерируй код для миграции Alembic.